# Только линт и формат — быстро (доли секунды на изменённых файлах).
#
# Сборку и тесты сюда НЕ кладём намеренно: хук, который думает минуту,
# начинают обходить через --no-verify к концу первой недели, и тогда
# не работает вообще ничего. Тяжёлое проверяет CI, у которого есть время.
pnpm lint-staged
