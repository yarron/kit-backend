# Упражнения

Порядок не случайный: каждое следующее опирается на предыдущее. Делай по одному,
и после каждого — `pnpm check && pnpm build && pnpm test:all`.

Правило на все упражнения: **тест пишется до кода**, и первый тест — на самый
дорогой сценарий, а не на happy path.

---

## 0. Разогрев: прочитать и запустить

1. Подними всё по README, открой `/gql` и создай пользователя.
2. Открой `/que` — там пусто. Создай заказ на $50 и обнови страницу:
   в очереди `order` появится job.
3. Создай заказ на $3. Он **не** попадёт в очередь сразу — он ниже порога.
   Подожди минуту (крон `scheduleFlush`) и посмотри, что произошло.
4. Создай заказ на $6000. Он упадёт: фейковый провайдер отдаёт 503. Посмотри
   в BullBoard, сколько было попыток и с какими интервалами.

**Вопрос, на который надо уметь ответить:** почему заказ на $3 не потерялся?

---

## 1. Новый CRUD-модуль

Сделай модуль `product`: `_id`, `sku` (уникальный), `title`, `priceUsd`,
`isActive`, soft-delete.

1. Скопируй `src/modules/user/`, переименуй.
2. Добавь в `src/modules/modules.ts`.
3. Тесты: дубликат `sku` → 409; обновление с `undefined` не затирает поле;
   удалённый товар не виден в списке.

**Проверь себя:** попробуй в playground отсортировать список по несуществующей
колонке `; drop`. Разберись по коду, почему ничего не произошло.

---

## 2. Связь между модулями, и почему запрос не живёт в резолвере поля

Добавь в `OrderEntity` поле `productId` и научись отдавать товар внутри заказа:

```graphql
query { orders(payload:{}) { items { _id product { title priceUsd } } } }
```

Упражнение из двух ходов, и второй важнее первого.

**Ход 1 — сделай неправильно и ИЗМЕРЬ.** Напиши `@ResolveField`, который ходит
за товаром в базу. Повесь счётчик (`mongoose.set("debug", …)` или просто
инкремент в сервисе), запроси 25 заказов и посчитай запросы. Их будет 26.
Не «медленно» — именно 26, и на месте вызова этого не видно вообще.

**Ход 2 — убери запрос из резолвера поля.** Собери `productId` всех заказов в
корневом резолвере, возьми товары одним `$in`, разложи по заказам. Счётчик
покажет 2. Сравни не только числа, но и диффы: во втором варианте видно,
сколько элементов обрабатывается, а в первом — нет.

Это и есть правило, которое мы держим во всех проектах: **в резолвере поля не
ходим в базу, никогда**. Не потому, что N+1 нечем лечить, а потому, что
правило снимает вопрос целиком и проверяется взглядом на диф. В стартере
поэтому нет ни одного `@ResolveField` с запросом.

**Про DataLoader.** Ты встретишь его в любом руководстве по GraphQL как
«решение N+1». Мы его не используем: это кэш на время запроса со своим
жизненным циклом, request-scope, требованием примитивных ключей и ручным
порядком результатов — цена за задачу, которой не возникает. А в Postgres её
и так нет: Prisma склеивает `findUnique` одного тика в один
`WHERE id IN (…)` сама (проверено: 5 вызовов → 1 SELECT).

---

## 3. Своя чистая функция + исчерпывающий тест

Добавь в `order.helpers.ts` функцию расчёта скидки:

- до $100 — 0%
- $100–499 — 5%
- $500+ — 10%
- у промо-кода свой процент, но суммарная скидка не больше 30%

Тест пиши **первым**. Обязательные случаи: ровно $100, $99.99, $500, $0,
отрицательная сумма, `NaN`, промо, который в сумме выходит за 30%, и результат,
у которого получается третий знак после запятой.

Смотри `src/utils/money.util.spec.ts` — там показано, какие случаи ловят баги,
а какие только создают ощущение покрытия.

---

## 4. Новый job в существующей очереди

Заказы в статусе `Queued` дольше 15 минут — это застрявшие: воркер умер после
claim. Сделай job `RECOVER_STUCK_ORDERS` в очереди `MAINTENANCE`, который
находит их и возвращает в `Pending`.

**Обрати внимание:** новая очередь тут НЕ нужна, нужно новое имя job'а
(см. правила в `CLAUDE.md`).

Тест обязан быть **многотактовым**: заказ застрял → job вернул его в Pending →
следующий flush его подобрал → он исполнился один раз.

---

## 4.5. Своя модель в Postgres — и что делает сборщик схемы

Заведи модуль `audit` со своей таблицей (`id`, `action`, `entityId`,
`createdAt`) — **на Prisma**, рядом с `invoice`.

1. Положи `src/modules/audit/audit.prisma` — только модель, без `generator`
   и `datasource`: они в шапке, которую добавляет сборщик.
2. `pnpm prisma:sync` и открой `prisma/schema.prisma`. Твоя модель должна
   появиться отдельным блоком с пометкой, из какого файла она пришла.
3. `pnpm prisma:local:gen` — создастся миграция.

**Что надо увидеть своими глазами:**

- запусти `pnpm prisma:sync` **дважды** — второй раз он скажет
  «уже актуален» и ничего не перезапишет. Почему это важно, а не мелочь?
- назови модель так же, как уже существующую (`Invoice`), и запусти
  `pnpm prisma:generate`. Прочитай ошибку. Где она случилась — на сборке
  схемы или в Prisma? Хорошо это или плохо, что она случилась именно там?
- удали свой модуль целиком и пересобери схему. Что стало с моделью,
  а что — с миграцией? Почему по-разному?

## 5. Аналитика

1. Добавь миграцию: колонка `discountUsd` в `order_event`.
   (Подумай: что произойдёт со **старыми** строками, у которых её нет?)
2. Пиши событие `CREATED` при создании заказа, а не только `FULFILLED`.
3. Новый GraphQL-запрос: конверсия по дням — сколько создано, сколько
   исполнено, отношение.

**Ловушка, которую надо пройти:** запусти один и тот же ingest дважды и
посмотри на числа с `FINAL` и без него. Объясни разницу.

---

## 6. E2E на полный путь заказа

Один тест: создать заказ → выполнить job вручную (не ждать крон) → проверить,
что статус `Fulfilled`, событие записалось в ClickHouse, а повторный запуск того
же job'а **не** создал второе событие.

Здесь ты столкнёшься с тем, что e2e с очередями требует управления временем.
Разберись, как запустить обработчик, не поднимая воркер.

---

## 7. Безопасность

1. Замени `ApiKeyGuard` на JWT: логин выдаёт токен, guard его проверяет.
2. Добавь роли: `userDeactivate` доступен только `Admin`.
3. Добавь rate limiting (`@nestjs/throttler`) на мутации.

Тесты: истёкший токен, токен с подменённой подписью, обычный пользователь
пытается удалить чужого.

---

## 8. Наблюдаемость

1. Подключи Sentry, залей source maps при сборке, урони эндпоинт специально
   и убедись, что стектрейс в Sentry читаемый (не минифицированный).
2. Добавь в `/health` проверку ClickHouse.
3. Сделай так, чтобы job, упавший все попытки, писал алерт (лог `ERROR` с
   контекстом достаточно; в бою это Telegram или Slack).

---

## Как понять, что ты вырос

- Ты пишешь тест на граничное значение **до** того, как написал функцию.
- Когда что-то не работает — ты идёшь смотреть в базу и в BullBoard, а не
  перечитываешь код по третьему разу.
- Ты можешь объяснить, **при каких условиях** твоё решение перестанет работать.
- Прежде чем добавлять поле, ты проверяешь грепом, кто ещё читает соседние.
- На вопрос «работает?» ты отвечаешь выводом команды, а не словом «да».
